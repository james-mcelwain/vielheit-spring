import React from 'react'
import './HomeView.scss'
import SideMenu from '../../../components/Menu/Menu'

export const HomeView = () => (
  <div>
    <div style={{ float: 'right', borderBottom: '1px dashed black' }}>
      <i><b>the animal is in the world like water in water. ... that is essentially on a level with the world in which it moves like water in water</b></i>
    </div>

    <SideMenu style={{ float: 'left' }} />
  </div>
)

export default HomeView
